name: CI Foundation & Regression Gate

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validation & Non-Regression Gate
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      # The repository intentionally has no npm lockfile yet, so npm ci and
      # setup-node's npm cache are not valid here. Keep CI aligned with the
      # repository's current dependency-management contract.
      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Frontend typecheck
        run: npm run typecheck

      - name: Backend typecheck
        run: npm run typecheck:server

      - name: Unit tests
        run: npm test

      - name: API integration tests
        run: npm run test:api

      - name: Server smoke test
        run: npm run test:smoke

      - name: Security hardening tests
        run: npm run test:security

      - name: ESI hardening & mock tests
        run: npm run test:esi

      - name: Production build
        run: npm run build
